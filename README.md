# Tutorial terraform repo
>  Esse repositório foi criado como treinamento para a semana devops elite. 

## Comandos 

### SSH
```
$ ssh-keygen -t rsa -b 2048
$ ssh -i [path_to_key] root@[address]
```

### Terraform 
```
$ terraform init
$ terraform validate
$ terraform apply
```